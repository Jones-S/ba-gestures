(function ($) { // iief = Immediately-Invoked Function Expression, mainly useful to limit scope
    $(function() { // Shorthand for $( document ).ready()

        // make shorter references to the global object
        var segs = LMGBA.segments,
            gests = LMGBA.gestures;

        /**
         * executor is called on every frame and
         * triggers the segments code fragments
         * @type {Object}
         */
        var executor = {
            execute: function () {
                asdf
            }
        };


    });
}(jQuery));